# SystemSextant CLI

This package contains the local interactive interface for SystemSextant. It is currently run from the repository root:

```bash
pnpm install
pnpm build
node packages/cli/dist/bin/systemsextant.js
```

Published package commands will be documented after release verification.
